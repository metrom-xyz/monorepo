{
    "type": "module",
    "private": true,
    "name": "carbon-subgraph",
    "scripts": {
        "prepare:deploy": "tsx ./scripts/prepare-deploy.ts",
        "codegen": "graph codegen",
        "deploy:local": "npm prepare:deploy sepolia carbon && graph codegen && graph build && graph create metrom-carbon-test --node http://localhost:8020 && graph deploy metrom-carbon-test --node http://localhost:8020 --ipfs http://localhost:5001 -l 0.0.1",
        "deploy:sepolia": "npm prepare:deploy sepolia carbon && graph codegen && graph build && graph deploy metrom-carbon-sepolia -l 0.0.62",
        "deploy:sei": "npm prepare:deploy sei-mainnet carbon && graph codegen && graph build && graph deploy metrom-carbon-sei -l 0.0.72",
        "lint": "eslint .",
        "format": "eslint --fix ."
    },
    "dependencies": {
        "@graphprotocol/graph-cli": "^0.97.1",
        "@graphprotocol/graph-ts": "0.38.1"
    },
    "devDependencies": {
        "@eslint/js": "^9.34.0",
        "@types/mustache": "^4.2.6",
        "@types/node": "^24.3.1",
        "eslint": "^9.34.0",
        "eslint-config-prettier": "^10.1.8",
        "eslint-config-turbo": "^2.5.6",
        "eslint-plugin-prettier": "^5.5.4",
        "mustache": "^4.2.0",
        "prettier": "^3.6.2",
        "tsx": "^4.20.5",
        "typescript": "^5.9.2",
        "typescript-eslint": "^8.42.0"
    }
}
