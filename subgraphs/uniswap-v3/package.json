{
    "type": "module",
    "private": true,
    "name": "uniswap-v3-subgraph",
    "scripts": {
        "prepare:deploy": "tsx ./scripts/prepare-deploy.ts",
        "codegen": "graph codegen",
        "deploy:uni-v3:base-sepolia": "npm run prepare:deploy base-sepolia uni-v3 && npm run codegen && graph deploy metrom-uni-v3-base-sepolia -l 0.0.21",
        "deploy:uni-v3:scroll": "npm run prepare:deploy scroll uni-v3 && npm run codegen && graph deploy metrom-uni-v3-scroll -l 0.0.17",
        "deploy:uni-v3:taiko": "npm run prepare:deploy taiko uni-v3 && npm run codegen && graph build && goldsky subgraph deploy metrom-uni-v3-taiko/0.0.12",
        "deploy:baseswap:base": "npm run prepare:deploy base baseswap && npm run codegen && graph deploy metrom-baseswap-base -l 0.0.12",
        "deploy:uni-v3:base": "npm run prepare:deploy base uni-v3 && npm run codegen && graph deploy metrom-uni-v3-base -l 0.0.12",
        "deploy:uni-v3:sonic": "npm run prepare:deploy sonic uni-v3 && npm run codegen && graph deploy metrom-uni-v-3-sonic -l 0.0.6",
        "deploy:unagi:taiko": "npm run prepare:deploy taiko unagi && npm run codegen && graph build && goldsky subgraph deploy metrom-unagi-taiko/0.0.7",
        "deploy:uni-v3:telos": "npm run prepare:deploy telos uni-v3 && npm run codegen && graph build && goldsky subgraph deploy metrom-uni-v3-telos/0.0.4",
        "deploy:uni-v3:lightlink-phoenix": "npm run prepare:deploy lightlink-phoenix uni-v3 && npm run codegen && graph create --node https://graph.phoenix.lightlink.io/rpc metrom-uni-v3-lightlink-phoenix-v4 && graph build && graph deploy --node https://graph.phoenix.lightlink.io/rpc metrom-uni-v3-lightlink-phoenix-v4 -l 0.0.1",
        "deploy:honeypop:scroll": "npm run prepare:deploy scroll honeypop && npm run codegen && graph deploy metrom-honeypop-scroll -l 0.0.6",
        "deploy:uni-v3:hemi": "npm run prepare:deploy hemi uni-v3 && npm run codegen && graph deploy metrom-uni-v-3-hemi -l 0.0.4",
        "deploy:uni-v3:gnosis": "npm run prepare:deploy gnosis uni-v3 && npm run codegen && graph deploy metrom-uni-v-3-gnosis -l 0.0.1",
        "deploy:uni-v3:lens": "npm run prepare:deploy lens uni-v3 && npm run codegen && graph deploy metrom-uni-v-3-lens -l 0.0.1",
        "deploy:taraswap:taraxa": "npm run prepare:deploy taraxa taraswap && npm run codegen && metrom-graph deploy production metrom-taraswap-taraxa 0.0.1",
        "deploy:uni-v3:arbitrum": "npm run prepare:deploy arbitrum-one uni-v3 && npm run codegen && graph deploy metrom-uni-v3-arbitrum -l 0.0.1",
        "lint": "eslint .",
        "format": "eslint --fix ."
    },
    "dependencies": {
        "@0xgraph/cli": "^0.2.1",
        "@graphprotocol/graph-cli": "^0.97.1",
        "@graphprotocol/graph-ts": "0.38.1",
        "@metrom-xyz/graph-cli": "*"
    },
    "devDependencies": {
        "@eslint/js": "^9.34.0",
        "@types/mustache": "^4.2.6",
        "@types/node": "^24.3.1",
        "eslint": "^9.34.0",
        "eslint-config-prettier": "^10.1.8",
        "eslint-config-turbo": "^2.5.6",
        "eslint-plugin-prettier": "^5.5.4",
        "mustache": "^4.2.0",
        "prettier": "^3.6.2",
        "tsx": "^4.20.5",
        "typescript": "^5.9.2",
        "typescript-eslint": "^8.42.0"
    }
}
