{
    "type": "module",
    "private": true,
    "name": "uniswap-v4-subgraph",
    "scripts": {
        "prepare:deploy": "tsx ./scripts/prepare-deploy.ts",
        "codegen": "graph codegen",
        "deploy:uni-v4:base-sepolia": "pnpm prepare:deploy base-sepolia uni-v4 && pnpm codegen && graph deploy metrom-uni-v-4-base-sepolia -l 0.0.3",
        "deploy:uni-v4:scroll": "pnpm prepare:deploy scroll uni-v4 && pnpm codegen && graph deploy metrom-uni-v4-scroll -l 0.0.17",
        "deploy:uni-v4:taiko": "pnpm prepare:deploy taiko uni-v4 && pnpm codegen && graph build && goldsky subgraph deploy metrom-uni-v4-taiko/0.0.12",
        "deploy:baseswap:base": "pnpm prepare:deploy base baseswap && pnpm codegen && graph deploy metrom-baseswap-base -l 0.0.12",
        "deploy:uni-v4:telos": "pnpm prepare:deploy telos uni-v4 && pnpm codegen && graph build && goldsky subgraph deploy metrom-uni-v4-telos/0.0.4",
        "deploy:uni-v4:lightlink-phoenix": "pnpm prepare:deploy lightlink-phoenix uni-v4 && pnpm codegen && graph create --node https://graph.phoenix.lightlink.io/rpc metrom-uni-v4-lightlink-phoenix-v4 && graph build && graph deploy --node https://graph.phoenix.lightlink.io/rpc metrom-uni-v4-lightlink-phoenix-v4 -l 0.0.1",
        "deploy:uni-v4:hemi": "pnpm prepare:deploy hemi uni-v4 && pnpm codegen && graph deploy metrom-uni-v-3-hemi -l 0.0.4",
        "deploy:uni-v4:gnosis": "pnpm prepare:deploy gnosis uni-v4 && pnpm codegen && graph deploy metrom-uni-v-3-gnosis -l 0.0.1",
        "deploy:uni-v4:lens": "pnpm prepare:deploy lens uni-v4 && pnpm codegen && graph deploy metrom-uni-v-3-lens -l 0.0.1",
        "lint": "eslint .",
        "format": "eslint --fix ."
    },
    "dependencies": {
        "@0xgraph/cli": "^0.2.1",
        "@graphprotocol/graph-cli": "^0.97.1",
        "@graphprotocol/graph-ts": "0.36.0"
    },
    "devDependencies": {
        "@eslint/js": "^9.27.0",
        "@types/mustache": "^4.2.6",
        "@types/node": "^24.0.1",
        "eslint": "^9.27.0",
        "eslint-config-prettier": "^10.1.5",
        "eslint-config-turbo": "^2.5.2",
        "eslint-plugin-prettier": "^5.4.0",
        "mustache": "^4.2.0",
        "prettier": "^3.5.3",
        "tsx": "^4.20.3",
        "typescript": "^5.8.3",
        "typescript-eslint": "^8.34.0"
    }
}
