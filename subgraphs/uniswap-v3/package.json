{
    "type": "module",
    "private": true,
    "name": "uniswap-v3-subgraph",
    "scripts": {
        "prepare:deploy": "tsx ./scripts/prepare-deploy.ts",
        "codegen": "graph codegen",
        "deploy:celo-alfajores": "pnpm prepare:deploy celo-alfajores && pnpm codegen && graph build && graph deploy --studio metrom-uni-v3-celo-alfajores --l 0.26.0",
        "deploy:base-sepolia": "pnpm prepare:deploy base-sepolia && pnpm codegen && graph build && graph deploy --studio metrom-uni-v3-base-sepolia -l 0.0.1",
        "deploy:arbitrum": "pnpm prepare:deploy arbitrum-one && pnpm codegen && graph build && graph deploy --studio metrom-uni-v3-arbitrum",
        "deploy:taiko": "pnpm prepare:deploy taiko && pnpm codegen && graph build && goldsky subgraph deploy metrom-uni-v3-taiko/0.0.1",
        "lint": "eslint .",
        "format": "eslint --fix ."
    },
    "dependencies": {
        "@graphprotocol/graph-cli": "0.85.0",
        "@graphprotocol/graph-ts": "0.35.1"
    },
    "devDependencies": {
        "@eslint/js": "^9.11.1",
        "@types/eslint__js": "^8.42.3",
        "@types/mustache": "^4.2.5",
        "@types/node": "^22.7.4",
        "eslint": "^8",
        "eslint-config-prettier": "^9.1.0",
        "eslint-config-turbo": "^2.1.3",
        "eslint-plugin-prettier": "^5.2.1",
        "mustache": "^4.2.0",
        "prettier": "^3.3.3",
        "tsx": "^4.19.1",
        "typescript": "^5.6.2",
        "typescript-eslint": "^8.8.0"
    }
}
