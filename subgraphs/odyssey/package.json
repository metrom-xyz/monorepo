{
    "type": "module",
    "private": true,
    "name": "odyssey-subgraph",
    "scripts": {
        "prepare:deploy": "tsx ./scripts/prepare-deploy.ts",
        "codegen": "graph codegen",
        "deploy:base": "pnpm prepare:deploy base && pnpm codegen && graph deploy metrom-odyssey-base -l 0.0.6",
        "deploy:mainnet": "pnpm prepare:deploy mainnet && pnpm codegen && graph deploy metrom-odyssey-mainnet -l 0.0.8",
        "lint": "eslint .",
        "format": "eslint --fix ."
    },
    "dependencies": {
        "@graphprotocol/graph-cli": "catalog:",
        "@graphprotocol/graph-ts": "catalog:"
    },
    "devDependencies": {
        "@eslint/js": "catalog:",
        "@types/mustache": "catalog:",
        "@types/node": "catalog:",
        "eslint": "catalog:",
        "eslint-config-prettier": "catalog:",
        "eslint-config-turbo": "catalog:",
        "eslint-plugin-prettier": "catalog:",
        "mustache": "catalog:",
        "prettier": "catalog:",
        "tsx": "catalog:",
        "typescript": "catalog:",
        "typescript-eslint": "catalog:"
    }
}
