{
    "type": "module",
    "private": true,
    "name": "liquity-v2-subgraph",
    "scripts": {
        "prepare:deploy": "tsx ./scripts/prepare-deploy.ts",
        "codegen": "graph codegen",
        "deploy:liquity:sepolia": "pnpm prepare:deploy sepolia liquity && pnpm codegen && graph deploy metrom-liquity-sepolia -l 0.0.12",
        "deploy:quill:scroll": "pnpm prepare:deploy scroll quill && pnpm codegen && graph deploy  metrom-quill-scroll -l 0.0.7",
        "deploy:orki:swell": "pnpm prepare:deploy swellchain orki && pnpm codegen && graph deploy metrom-orki-swell -l 0.0.4",
        "deploy:liquity:mainnet": "pnpm prepare:deploy mainnet liquity && pnpm codegen && graph deploy metrom-liquity-mainnet -l 0.0.2",
        "lint": "eslint .",
        "format": "eslint --fix ."
    },
    "dependencies": {
        "@0xgraph/cli": "^0.2.1",
        "@graphprotocol/graph-cli": "catalog:",
        "@graphprotocol/graph-ts": "catalog:"
    },
    "devDependencies": {
        "@eslint/js": "catalog:",
        "@types/mustache": "catalog:",
        "@types/node": "catalog:",
        "eslint": "catalog:",
        "eslint-config-prettier": "catalog:",
        "eslint-plugin-prettier": "catalog:",
        "mustache": "catalog:",
        "prettier": "catalog:",
        "tsx": "catalog:",
        "typescript": "catalog:",
        "typescript-eslint": "catalog:"
    }
}
