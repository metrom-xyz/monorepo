{
    "type": "module",
    "private": true,
    "name": "liquity-v2-subgraph",
    "scripts": {
        "prepare:deploy": "tsx ./scripts/prepare-deploy.ts",
        "codegen": "graph codegen",
        "deploy:liquity:sepolia": "npm run prepare:deploy sepolia liquity && npm run codegen && graph deploy --studio metrom-liquity-sepolia -l 0.0.11",
        "deploy:ebisu:sepolia": "npm run prepare:deploy sepolia ebisu && npm run codegen && graph deploy --studio metrom-ebisu-sepolia -l 0.0.2",
        "deploy:quill:scroll": "npm run prepare:deploy scroll quill && npm run codegen && graph deploy --studio  metrom-quill-scroll -l 0.0.7",
        "deploy:orki:swell": "npm run prepare:deploy swellchain orki && npm run codegen && graph deploy --studio metrom-orki-swell -l 0.0.4",
        "deploy:ebisu:mainnet": "npm run prepare:deploy mainnet ebisu && npm run codegen && graph deploy --studio metrom-ebisu-mainnet -l 0.0.1",
        "deploy:liquity:mainnet": "npm run prepare:deploy mainnet liquity && npm run codegen && graph deploy --studio metrom-liquity-mainnet -l 0.0.2",
        "deploy:ebisu:plasma": "npm run prepare:deploy plasma-mainnet ebisu && npm run codegen && graph build && goldsky subgraph deploy metrom-ebisu-plasma/0.0.1",
        "lint": "eslint .",
        "format": "eslint --fix ."
    },
    "dependencies": {
        "@0xgraph/cli": "^0.2.1",
        "@graphprotocol/graph-cli": "^0.98.1",
        "@graphprotocol/graph-ts": "0.38.1"
    },
    "devDependencies": {
        "@eslint/js": "^9.34.0",
        "@types/mustache": "^4.2.6",
        "@types/node": "^24.3.1",
        "eslint": "^9.34.0",
        "eslint-config-prettier": "^10.1.8",
        "eslint-plugin-prettier": "^5.5.4",
        "mustache": "^4.2.0",
        "prettier": "^3.6.2",
        "tsx": "^4.20.5",
        "typescript": "^5.9.2",
        "typescript-eslint": "^8.42.0"
    }
}
