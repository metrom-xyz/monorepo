{
    "type": "module",
    "private": true,
    "name": "aave-v3-subgraph",
    "scripts": {
        "prepare:deploy": "tsx ./scripts/prepare-deploy.ts",
        "codegen": "graph codegen",
        "deploy:ploutos:arbitrum": "pnpm prepare:deploy arbitrum-one ploutos && pnpm codegen && graph deploy metrom-ploutos-arbitrum -l 0.0.8",
        "deploy:purrlend:megaeth": "pnpm prepare:deploy megaeth purrlend && pnpm codegen && graph deploy metrom-purrlend-megaeth -l 0.0.1",
        "lint": "eslint .",
        "format": "eslint --fix ."
    },
    "dependencies": {
        "@graphprotocol/graph-cli": "catalog:",
        "@graphprotocol/graph-ts": "catalog:"
    },
    "devDependencies": {
        "@eslint/js": "catalog:",
        "@types/mustache": "catalog:",
        "@types/node": "catalog:",
        "eslint": "catalog:",
        "eslint-config-prettier": "catalog:",
        "eslint-plugin-prettier": "catalog:",
        "mustache": "catalog:",
        "prettier": "catalog:",
        "tsx": "catalog:",
        "typescript": "catalog:",
        "typescript-eslint": "catalog:"
    }
}
