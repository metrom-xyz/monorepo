{
    "type": "module",
    "private": true,
    "name": "velodrome-subgraph",
    "scripts": {
        "prepare:deploy": "tsx ./scripts/prepare-deploy.ts",
        "codegen": "graph codegen",
        "deploy:velodrome:swell": "pnpm prepare:deploy swellchain velodrome && pnpm codegen && graph deploy --studio metrom-velodrome-swell -l 0.0.27",
        "deploy:aerodrome:base": "pnpm prepare:deploy base aerodrome && pnpm codegen && graph deploy --studio metrom-aerodrome-base -l 0.0.2",
        "deploy:velodrome:ink": "pnpm prepare:deploy ink velodrome && pnpm codegen && graph build && goldsky subgraph deploy metrom-velodrome-ink/0.0.1",
        "lint": "eslint .",
        "format": "eslint --fix ."
    },
    "dependencies": {
        "@graphprotocol/graph-cli": "catalog:",
        "@graphprotocol/graph-ts": "catalog:"
    },
    "devDependencies": {
        "@eslint/js": "catalog:",
        "@types/mustache": "catalog:",
        "@types/node": "catalog:",
        "eslint": "catalog:",
        "eslint-config-prettier": "catalog:",
        "eslint-config-turbo": "catalog:",
        "eslint-plugin-prettier": "catalog:",
        "mustache": "catalog:",
        "prettier": "catalog:",
        "tsx": "catalog:",
        "typescript": "catalog:",
        "typescript-eslint": "catalog:"
    }
}
