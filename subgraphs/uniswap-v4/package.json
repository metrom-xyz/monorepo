{
    "type": "module",
    "private": true,
    "name": "uniswap-v4-subgraph",
    "scripts": {
        "prepare:deploy": "tsx ./scripts/prepare-deploy.ts",
        "codegen": "graph codegen",
        "deploy:uni-v4:sepolia": "pnpm prepare:deploy sepolia uni-v4 && pnpm codegen && graph deploy metrom-uni-v-4-sepolia -l 0.0.29",
        "deploy:uni-v4:base": "pnpm prepare:deploy base uni-v4 && pnpm codegen && graph deploy metrom-uni-v-4-base -l 0.0.1",
        "deploy:uni-v4:megaeth": "pnpm prepare:deploy megaeth-mainnet uni-v4 && pnpm codegen && graph build && goldsky subgraph deploy metrom-uni-v4-megaeth/0.0.1",
        "lint": "eslint .",
        "format": "eslint --fix ."
    },
    "dependencies": {
        "@0xgraph/cli": "^0.2.1",
        "@graphprotocol/graph-cli": "catalog:",
        "@graphprotocol/graph-ts": "catalog:",
        "@metrom-xyz/graph-cli": "workspace:*"
    },
    "devDependencies": {
        "@eslint/js": "catalog:",
        "@types/mustache": "catalog:",
        "@types/node": "catalog:",
        "eslint": "catalog:",
        "eslint-config-prettier": "catalog:",
        "eslint-config-turbo": "catalog:",
        "eslint-plugin-prettier": "catalog:",
        "mustache": "catalog:",
        "prettier": "catalog:",
        "tsx": "catalog:",
        "typescript": "catalog:",
        "typescript-eslint": "catalog:"
    }
}
