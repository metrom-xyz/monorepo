{
    "type": "module",
    "private": true,
    "name": "metrom-subgraph",
    "scripts": {
        "prepare:deploy": "tsx ./scripts/prepare-deploy.ts",
        "codegen": "graph codegen",
        "deploy:holesky": "npm run prepare:deploy holesky && npm run codegen && graph deploy metrom-holesky -l=v0.0.28",
        "deploy:base-sepolia": "npm run prepare:deploy base-sepolia && npm run codegen && graph deploy metrom-base-sepolia -l=v0.0.7",
        "deploy:sepolia": "npm run prepare:deploy sepolia && npm run codegen && graph deploy metrom-sepolia -l=v0.0.4",
        "deploy:base": "npm run prepare:deploy base && npm run codegen && graph deploy metrom-base -l 0.0.20",
        "deploy:mode": "npm run prepare:deploy mode-mainnet && npm run codegen && graph deploy metrom-mode -l 0.0.11",
        "deploy:mantle": "npm run prepare:deploy mantle && npm run codegen && 0xgraph deploy metrom-mantle -l 0.0.7",
        "deploy:taiko": "npm run prepare:deploy taiko && npm run codegen && graph build && goldsky subgraph deploy metrom-taiko/0.0.8",
        "deploy:scroll": "npm run prepare:deploy scroll && npm run codegen && graph deploy metrom-scroll -l 0.0.8",
        "deploy:sonic": "npm run prepare:deploy sonic && npm run codegen && graph deploy metrom-sonic -l 0.0.1",
        "deploy:form": "npm run prepare:deploy form && npm run codegen && graph deploy metrom-form -l 0.0.7 --node https://formapi.0xgraph.xyz/deploy --ipfs https://formapi.0xgraph.xyz/ipfs",
        "deploy:gnosis": "npm run prepare:deploy gnosis && npm run codegen && graph deploy metrom-gnosis -l 0.0.2",
        "deploy:telos": "npm run prepare:deploy telos && npm run codegen && graph build && graph deploy metrom-telos/0.0.1",
        "deploy:lightlink-phoenix": "npm run prepare:deploy lightlink-phoenix && npm run codegen && graph create --node https://graph.phoenix.lightlink.io/rpc metrom-lightlink-phoenix && graph build && graph deploy --node https://graph.phoenix.lightlink.io/rpc metrom-lightlink-phoenix -l 0.0.1",
        "deploy:sei": "npm run prepare:deploy sei-mainnet && npm run codegen && graph build && graph deploy metrom-sei -l 0.0.2",
        "deploy:swell": "npm run prepare:deploy swellchain && npm run codegen && graph deploy metrom-swell -l 0.0.2",
        "deploy:hemi": "npm run prepare:deploy hemi && npm run codegen && graph deploy metrom-hemi -l 0.0.1",
        "deploy:lens": "npm run prepare:deploy lens && npm run codegen && graph deploy metrom-lens -l 0.0.1",
        "deploy:lumia": "npm run prepare:deploy lumia && npm run codegen && graph deploy metrom-lumia -l 0.0.1",
        "deploy:arbitrum-one": "npm run prepare:deploy arbitrum-one && npm run codegen && graph deploy metrom-arbitrum-one -l 0.0.1",
        "deploy:plasma": "npm run prepare:deploy plasma-mainnet && npm run codegen && graph build && goldsky subgraph deploy metrom-plasma/0.0.1",
        "deploy:saga": "npm run prepare:deploy saga-evm && npm run codegen && graph build && goldsky subgraph deploy metrom-saga/0.0.1",
        "lint": "eslint .",
        "format": "eslint --fix ."
    },
    "dependencies": {
        "@0xgraph/cli": "^0.2.1",
        "@graphprotocol/graph-cli": "^0.97.1",
        "@graphprotocol/graph-ts": "0.38.1",
        "@metrom-xyz/graph-cli": "*"
    },
    "devDependencies": {
        "@eslint/js": "^9.34.0",
        "@metrom-xyz/contracts": "^0.53.0",
        "@types/mustache": "^4.2.6",
        "@types/node": "^24.3.1",
        "eslint": "^9.34.0",
        "eslint-config-prettier": "^10.1.8",
        "eslint-plugin-prettier": "^5.5.4",
        "mustache": "^4.2.0",
        "prettier": "^3.6.2",
        "tsx": "^4.20.5",
        "typescript": "^5.9.2",
        "typescript-eslint": "^8.42.0"
    }
}
