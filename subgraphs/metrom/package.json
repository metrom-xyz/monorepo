{
    "type": "module",
    "private": true,
    "name": "subgraph",
    "scripts": {
        "prepare:deploy": "tsx ./scripts/prepare-deploy.ts",
        "codegen": "graph codegen",
        "deploy:holesky": "pnpm prepare:deploy holesky && pnpm codegen && graph deploy --studio metrom-holesky -l=v0.0.21",
        "deploy:celo-alfajores": "pnpm prepare:deploy celo-alfajores && pnpm codegen && graph deploy --studio metrom-celo-alfajores -l=v0.0.20",
        "deploy:mantle-sepolia": "pnpm prepare:deploy mantle-sepolia && pnpm codegen && graph build && graph deploy metrom-mantle-sepolia -l 0.0.1 --node https://subgraph-api.mantle.xyz/deploy --ipfs https://subgraph-api.mantle.xyz/ipfs",
        "deploy:sonic-testnet": "pnpm prepare:deploy sonic && pnpm codegen && graph build && graph deploy luzzif/metrom-sonic-testnet -l 0.0.1 --node https://graph.testnet.soniclabs.com/service --ipfs https://graph.testnet.soniclabs.com/",
        "deploy:base-sepolia": "pnpm prepare:deploy base-sepolia && pnpm codegen && graph deploy --studio metrom-base-sepolia -l=v0.0.1",
        "deploy:base": "pnpm prepare:deploy base && pnpm codegen && graph build && graph deploy --studio metrom-base -l 0.0.2",
        "deploy:mode": "pnpm prepare:deploy mode-mainnet && pnpm codegen && graph build && graph deploy --studio metrom-mode -l 0.0.3",
        "deploy:mantle": "pnpm prepare:deploy mantle && pnpm codegen && graph build && graph deploy metrom-mantle -l 0.0.2 --node https://subgraph-api.mantle.xyz/deploy --ipfs https://subgraph-api.mantle.xyz/ipfs",
        "deploy:taiko": "pnpm prepare:deploy taiko && pnpm codegen && graph build && goldsky subgraph deploy metrom-taiko/0.0.1",
        "deploy:scroll": "pnpm prepare:deploy scroll && pnpm codegen && graph build && graph deploy --studio metrom-scroll -l 0.0.1",
        "lint": "eslint .",
        "format": "eslint --fix ."
    },
    "dependencies": {
        "@graphprotocol/graph-cli": "0.85.0",
        "@graphprotocol/graph-ts": "0.35.1"
    },
    "devDependencies": {
        "@eslint/js": "^9.11.1",
        "@metrom-xyz/contracts": "^0.28.0",
        "@types/eslint__js": "^8.42.3",
        "@types/mustache": "^4.2.5",
        "@types/node": "^22.7.4",
        "eslint": "^8",
        "eslint-config-prettier": "^9.1.0",
        "eslint-plugin-prettier": "^5.2.1",
        "mustache": "^4.2.0",
        "prettier": "^3.3.3",
        "tsx": "^4.19.1",
        "typescript": "^5.6.2",
        "typescript-eslint": "^8.8.0"
    }
}
