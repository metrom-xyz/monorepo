{
    "type": "module",
    "private": true,
    "name": "algebra-integral-subgraph",
    "scripts": {
        "prepare:deploy": "tsx ./scripts/prepare-deploy.ts",
        "codegen": "graph codegen",
        "deploy:test-integral:holesky": "pnpm prepare:deploy holesky test-integral && pnpm codegen && graph deploy --studio metrom-test-integral-holesky -l 0.0.33",
        "deploy:silverswap:sonic": "pnpm prepare:deploy sonic silverswap && pnpm codegen && graph deploy --studio metrom-silverswap-sonic -l 0.0.8",
        "deploy:fibonacci:form": "pnpm prepare:deploy form fibonacci && pnpm codegen && graph deploy --studio metrom-fibonacci-form -l 0.0.9 --node https://formapi.0xgraph.xyz/deploy --ipfs https://formapi.0xgraph.xyz/ipfs",
        "deploy:swapr:gnosis": "pnpm prepare:deploy gnosis swapr && pnpm codegen && graph deploy --studio metrom-swapr-gnosis -l 0.0.17",
        "deploy:hydrex:base": "pnpm prepare:deploy base hydrex && pnpm codegen && graph deploy metrom-hydrex-base -l 0.0.6",
        "lint": "eslint .",
        "format": "eslint --fix ."
    },
    "dependencies": {
        "@0xgraph/cli": "^0.2.1",
        "@graphprotocol/graph-cli": "catalog:",
        "@graphprotocol/graph-ts": "catalog:",
        "@metrom-xyz/graph-cli": "workspace:*"
    },
    "devDependencies": {
        "@eslint/eslintrc": "catalog:",
        "@eslint/js": "catalog:",
        "@types/mustache": "catalog:",
        "@types/node": "catalog:",
        "eslint": "catalog:",
        "eslint-config-prettier": "catalog:",
        "eslint-plugin-prettier": "catalog:",
        "mustache": "catalog:",
        "prettier": "catalog:",
        "tsx": "catalog:",
        "typescript": "catalog:",
        "typescript-eslint": "catalog:"
    }
}
