{
    "type": "module",
    "private": true,
    "name": "blocks-subgraph",
    "scripts": {
        "prepare:deploy": "tsx ./scripts/prepare-deploy.ts",
        "codegen": "graph codegen",
        "deploy:holesky": "npm prepare:deploy holesky && npm codegen && graph deploy metrom-blocks-holesky -l 0.0.8",
        "deploy:base-sepolia": "npm prepare:deploy base-sepolia && npm codegen && graph deploy metrom-base-sepolia-blocks -l 0.0.2",
        "deploy:sepolia": "npm prepare:deploy sepolia && npm codegen && graph deploy metrom-sepolia-blocks -l 0.0.1",
        "deploy:mantle": "npm prepare:deploy mantle && npm codegen && 0xgraph deploy metrom-mantle-blocks -l 0.0.1",
        "deploy:mode": "npm prepare:deploy mode-mainnet && npm codegen && graph deploy metrom-mode-blocks -l 0.0.1",
        "deploy:base": "npm prepare:deploy base && npm codegen && graph deploy --studio metrom-base-blocks -l 0.0.1",
        "deploy:taiko": "npm prepare:deploy taiko && npm codegen && graph build && goldsky subgraph deploy metrom-taiko-blocks/0.0.2",
        "deploy:scroll": "npm prepare:deploy scroll && npm codegen && graph deploy --studio metrom-scroll-blocks -l 0.0.1",
        "deploy:sonic": "npm prepare:deploy sonic && 0xgraph codegen && 0xgraph deploy metrom-sonic-blocks -l 0.0.2",
        "deploy:form": "npm prepare:deploy form && npm codegen && graph deploy metrom-form-blocks -l 0.0.1 --node https://formapi.0xgraph.xyz/deploy --ipfs https://formapi.0xgraph.xyz/ipfs",
        "deploy:gnosis": "npm prepare:deploy gnosis && npm codegen && graph deploy metrom-gnosis-blocks -l 0.0.1",
        "deploy:lightlink-phoenix": "npm prepare:deploy lightlink-phoenix && npm codegen && graph create --node https://graph.phoenix.lightlink.io/rpc metrom-lightlink-phoenix-blocks && graph build && graph deploy --node https://graph.phoenix.lightlink.io/rpc metrom-lightlink-phoenix-blocks -l 0.0.1",
        "lint": "eslint .",
        "format": "eslint --fix ."
    },
    "dependencies": {
        "@0xgraph/cli": "^0.2.1",
        "@graphprotocol/graph-cli": "^0.98.1",
        "@graphprotocol/graph-ts": "0.38.1",
        "@metrom-xyz/graph-cli": "*"
    },
    "devDependencies": {
        "@eslint/js": "^9.34.0",
        "@types/mustache": "^4.2.6",
        "@types/node": "^24.3.1",
        "eslint": "^9.34.0",
        "eslint-config-prettier": "^10.1.8",
        "eslint-config-turbo": "^2.5.6",
        "eslint-plugin-prettier": "^5.5.4",
        "mustache": "^4.2.0",
        "prettier": "^3.6.2",
        "tsx": "^4.20.5",
        "typescript": "^5.9.2",
        "typescript-eslint": "^8.42.0"
    }
}
