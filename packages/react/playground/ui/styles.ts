import type { CSSProperties } from "react";

export const sectionStyle: CSSProperties = {
    border: "1px solid #ccc",
    borderRadius: 4,
    padding: 12,
    marginBottom: 16,
};

export const inputsStyle: CSSProperties = {
    display: "flex",
    flexDirection: "column",
    gap: 8,
    marginBottom: 16,
};

export const preStyle: CSSProperties = {
    maxHeight: 300,
    overflow: "auto",
    background: "#f5f5f5",
    padding: 8,
};
