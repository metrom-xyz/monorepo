import type { TokenInfo } from "@uniswap/token-lists";

export interface PairRemoteLogoProps {
    sm?: boolean;
    lg?: boolean;
    xl?: boolean;
    xxl?: boolean;
    token0?: TokenInfo;
    token1?: TokenInfo;
}
