import deadbox from "./deadbox.webp";

// eslint-disable-next-line @typescript-eslint/no-explicit-any
export function DeadboxLogo(props: any) {
    return <img src={deadbox} alt="Deadbox logo" {...props} />;
}
