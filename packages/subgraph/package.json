{
    "type": "module",
    "private": true,
    "name": "subgraph",
    "scripts": {
        "prepare:deploy": "tsx ./scripts/prepare-deploy.ts",
        "codegen": "graph codegen",
        "deploy:holesky:dev": "pnpm prepare:deploy development holesky && pnpm codegen && graph deploy --studio metrom-holesky-dev",
        "deploy:holesky:staging": "pnpm prepare:deploy staging holesky && pnpm codegen && graph deploy --studio metrom-holesky-staging",
        "deploy:celo-alfajores:dev": "pnpm prepare:deploy development celo-alfajores && pnpm codegen && graph deploy --studio metrom-celo-alfajores-dev",
        "deploy:celo-alfajores:staging": "pnpm prepare:deploy staging celo-alfajores && pnpm codegen && graph deploy --studio metrom-celo-alfajores-staging",
        "deploy:mantle-sepolia:dev": "pnpm prepare:deploy development mantle-sepolia && pnpm codegen && graph build && goldsky subgraph deploy metrom-mantle-sepolia-dev/0.1.0",
        "deploy:mantle-sepolia:staging": "pnpm prepare:deploy staging mantle-sepolia && pnpm codegen && graph build && goldsky subgraph deploy metrom-mantle-sepolia-staging/0.1.0",
        "lint": "eslint .",
        "format": "eslint --fix ."
    },
    "dependencies": {
        "@graphprotocol/graph-cli": "0.78.0",
        "@graphprotocol/graph-ts": "0.35.1"
    },
    "devDependencies": {
        "@eslint/js": "^9.7.0",
        "@metrom-xyz/contracts": "^0.13.1",
        "@types/eslint__js": "^8.42.3",
        "@types/mustache": "^4.2.5",
        "@types/node": "^20.14.11",
        "eslint": "^9.7.0",
        "eslint-config-prettier": "^9.1.0",
        "eslint-plugin-prettier": "^5.2.1",
        "mustache": "^4.2.0",
        "prettier": "^3.3.3",
        "tsx": "^4.16.0",
        "typescript": "^5.5.3",
        "typescript-eslint": "^7.16.1"
    }
}
