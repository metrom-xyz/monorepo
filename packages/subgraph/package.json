{
    "type": "module",
    "private": true,
    "name": "subgraph",
    "scripts": {
        "update-addresses": "tsx ./scripts/update-addresses.ts",
        "codegen": "graph codegen",
        "build:only": "graph build",
        "build": "pnpm update-addresses holesky && pnpm codegen && graph build",
        "deploy:holesky": "pnpm update-addresses holesky && graph deploy --studio --network holesky metrom-holesky",
        "lint": "eslint --ext .js,.ts ./ && prettier --check './**/*.{json,md}'",
        "format": "eslint --fix --ext .js,.ts ./ && prettier --write './**/*.{json,md}'"
    },
    "dependencies": {
        "@graphprotocol/graph-cli": "0.71.2",
        "@graphprotocol/graph-ts": "0.35.1"
    },
    "devDependencies": {
        "@metrom-xyz/contracts": "^0.6.1",
        "@types/node": "^20.12.7",
        "@typescript-eslint/eslint-plugin": "^7.4.0",
        "@typescript-eslint/parser": "^7.4.0",
        "eslint": "^8.57.0",
        "eslint-config-prettier": "^9.1.0",
        "eslint-config-turbo": "^1.13.0",
        "eslint-plugin-prettier": "^5.1.3",
        "prettier": "^3.2.5",
        "tsx": "^4.7.2"
    }
}
